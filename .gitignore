# Audio files (samples / user media — not part of the app)
*.m4a
*.mp3
*.wav
*.aac
*.ogg
*.flac
*.opus

# OS / editor cruft
.DS_Store
*.log

# Deployment metadata (bucket / distribution IDs)
.deploy-info

# yt-dlp's own download-tracking state (per-machine, not app source)
downloads/.download-archive.txt
